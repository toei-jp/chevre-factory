/**
 * 取引ステータス
 */

enum TransactionStatusType {
    InProgress = 'InProgress',
    Canceled = 'Canceled',
    Confirmed = 'Confirmed',
    Expired = 'Expired'
}

export default TransactionStatusType;
